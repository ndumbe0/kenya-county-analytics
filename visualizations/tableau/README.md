# Tableau Dashboard Export

Use `kenya_county_overview_extract.csv` and `data/geospatial/kenya-counties.geojson` to build the Population Map, Economic Trends, Health Indicators, and Education Analysis sheets. Save the packaged workbook here as `kenya_county_overview.twbx`.
